# NPM configuration for @llm-dev-ops organization

# Access level for scoped packages
access=public

# Registry
registry=https://registry.npmjs.org/

# Always use exact versions for dependencies
save-exact=true

# Engine strict mode
engine-strict=true
